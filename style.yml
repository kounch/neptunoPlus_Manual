extends: default

header:
  height: 15mm
  font-family: M+ 1mn
  recto:
    right:
      content: "{document-title} ({revnumber}): {section-or-chapter-title}"
  verso:
    left:
      content: "{document-title} ({revnumber}): {section-or-chapter-title}"
