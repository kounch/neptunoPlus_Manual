extends: default

header:
  height: 15mm
  font-family: M+ 1mn
  recto:
    right:
      content: "{document-title} ({docdate}): {section-or-chapter-title}"
  verso:
    left:
      content: "{document-title} ({docdate}): {section-or-chapter-title}"
